const {gets, print} = require('./sorteio_guiado');

const quantidadeAluno = gets();
let maior = 0;

for(let i = 0; i < quantidadeAluno; i++){
    const entradas = gets();
    if(entradas > maior){
        maior = entradas;
    }
}

print(maior);