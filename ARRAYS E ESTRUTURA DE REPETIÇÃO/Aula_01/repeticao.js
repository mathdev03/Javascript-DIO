// Pode ser uma lista array de caracteres definido(Um array especial)
// Mas é apenas para leitura.
const nome = 'Matheus Yuji Setoguchi';

for (let index = 0; index < nome.length; index++) {
    const element = nome[index];
    console.log(element);
}